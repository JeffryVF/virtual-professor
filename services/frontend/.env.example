# Browser-facing API origin (no trailing slash).
# Local Docker (nginx): /api
# Local without nginx:  http://localhost:8000
# Vercel production:    https://YOUR-API.onrender.com
NEXT_PUBLIC_API_URL=/api
